org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
org.gradle.caching=true
android.useAndroidX=true
android.enableJetifier=true
# Kotlin's incremental cache can register the same plugin storage twice on
# Windows worktree paths. In-process non-incremental compilation fails loudly
# instead of falling back after thousands of daemon errors.
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
